.gradle
**/build/
!src/**/build/
.gradletasknamecache
gradle-app.setting

# IDE
.idea/
*.iml
.project
.classpath

# CI (managed locally, not needed by consumers)
.github/

# Gradle wrapper binary (binary, no value in repo)
gradle/wrapper/gradle-wrapper.jar
gradlew.bat
